import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flex: 1, padding: 15 },
  emptyListMessage: { fontSize: 20, fontWeight: "bold", fontFamily: "Roboto" },
});
